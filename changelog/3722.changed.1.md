Read the current AOUSD cable material attributes in the experimental USD deformable importer: `physics:curvesThickness` and the four structural `physics:curves*Stiffness` attributes. Resolve each unauthored stiffness from `physics:youngsModulus`, `physics:poissonsRatio`, and `physics:curvesThickness` instead of using `add_rod()` defaults. A curve material with unauthored thickness now uses AOUSD's 1 mm diameter fallback (0.5 mm radius) instead of Newton's previous 2.5 mm radius; short segments at this diameter may have their inertia corrected during model finalization. Re-test affected scenes and migrate earlier unprefixed attributes as described in the deprecation entry.
